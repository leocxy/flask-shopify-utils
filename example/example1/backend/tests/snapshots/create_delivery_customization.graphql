mutation CreateDeliveryCustomization {
  deliveryCustomizationCreate(deliveryCustomization: {functionHandle: "function_handle", title: "title", enabled: true, metafields: []}) {
    deliveryCustomization {
      id
    }
    userErrors {
      field
      message
      code
    }
  }
}
